require 'hoe'

Hoe.new 'rbayes', '1.0.0' do |p|
  p.summary = 'A BDB-using bayesian filter'
  p.description = ''
  p.author = 'Eric Hodel'
  p.email = 'drbrain@segment7.net'
  p.url = 'http://seattlerb.rubyforge.org/rbayes/'
end

# vim: syntax=Ruby
